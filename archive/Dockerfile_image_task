FROM python:3.9-slim

WORKDIR /app
RUN pip install --no-cache-dir Pillow psutil

COPY process_image.py .
# The image file will be mounted via volume for live processing

ENTRYPOINT ["python", "process_image.py"]
